.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# we use pnp but some tools (jest esbuild) keep a cache here by default
node_modules/

# Built artifacts
dist

# test coverage artifacts
coverage/